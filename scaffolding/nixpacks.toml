[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install -g pnpm@9", "pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "pnpm --filter @fvos/api exec tsx src/index.ts"
